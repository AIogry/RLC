study_id: M11A
config_id: M11A-C003
slug: crl_single_state_actor_ff_critic
algorithm: crl
placement: actor_computation
executable: true
description: CRL SingleState actor with feed-forward critic branches.
factors: {topology: single_state, actor_computation: single_state, critic_computation: feedforward}
agent_overrides:
  actor_loss: ddpgbc
  compute:
    actor: {enabled: true, primitive: mlp, topology: single_state, credit: direct, topology_kwargs: {iterations: 4, residual: false, input_injection: z_plus_x, state_dim: 512, state_init: normal_buffer, state_init_std: 1.0, update_depth: 2}}
    critic_state: {enabled: false, primitive: mlp, topology: feedforward, credit: direct}
    critic_goal: {enabled: false, primitive: mlp, topology: feedforward, credit: direct}
    value_state: {enabled: false, primitive: mlp, topology: feedforward, credit: direct}
    value_goal: {enabled: false, primitive: mlp, topology: feedforward, credit: direct}
