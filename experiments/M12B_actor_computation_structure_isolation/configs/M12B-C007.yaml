study_id: M12B
config_id: M12B-C007
slug: residual_ff_4x2
algorithm: crl
environment: antmaze-large-navigate-v0
stage: policy_extraction
protocol_stage: stage2
placement: actor_computation_structure
executable: true
condition_id: B009
description: FeedForward topology with four independent two-Dense residual blocks.
factors: {structure: residual_ff_4x2, state_init: null, parameter_sharing: null, residual_blocks: 4, block_depth: 2}
dependencies:
  frozen_critic:
    source_study_id: M12A
    source_study_path: ../M12A_frozen_critic_policy_extraction/study.yaml
    source_config_id: M12A-C001
    seed_policy: same_seed
    source_run_attempt: 0
    checkpoint_role: last
    checkpoint_step: 1000000
    module: critic
agent_overrides:
  actor_loss: ddpgbc
  training_mode: policy_extraction
  runtime_variant: policy_extractor
  compute:
    actor:
      enabled: true
      primitive: mlp
      block: residual
      block_kwargs: {state_dim: 512, blocks: 4, block_depth: 2, layer_norm: false, block_activate_final: true}
      topology: feedforward
      credit: direct
