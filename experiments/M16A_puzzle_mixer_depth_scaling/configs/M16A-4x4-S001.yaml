study_id: M16A
config_id: M16A-4x4-S001
slug: 4x4_mixer_l1
algorithm: gciql
environment: puzzle-4x4-play-v0
protocol_stage: formal
placement: actor+value+critic
executable: true
condition_id: S001
semantic_condition: S001_STRUCTURED_MIXER_L1
description: Structured Mixer L1 on puzzle-4x4-play-v0; seed 0.
factors: {environment: puzzle-4x4-play-v0, condition: S001, structure: puzzle_tokens, topology: feedforward, block: mlp_mixer, num_buttons: 16, num_mixer_blocks: 1}
agent_overrides:
  actor_loss: ddpgbc
  compute:
    actor:
      enabled: true
      primitive: mlp
      topology: feedforward
      block: mlp_mixer
      credit: direct
      structure: puzzle_tokens
      structure_kwargs: {num_buttons: 16, token_dim: 128, robot_hidden_dim: 128, token_mlp_hidden_dim: 64, channel_mlp_hidden_dim: 256, num_mixer_blocks: 1, index_embedding: true, readout: mean, tm_mode: none}
    value:
      enabled: true
      primitive: mlp
      topology: feedforward
      block: mlp_mixer
      credit: direct
      structure: puzzle_tokens
      structure_kwargs: {num_buttons: 16, token_dim: 128, robot_hidden_dim: 128, token_mlp_hidden_dim: 64, channel_mlp_hidden_dim: 256, num_mixer_blocks: 1, index_embedding: true, readout: mean, tm_mode: none}
    critic:
      enabled: true
      primitive: mlp
      topology: feedforward
      block: mlp_mixer
      credit: direct
      structure: puzzle_tokens
      structure_kwargs: {num_buttons: 16, token_dim: 128, robot_hidden_dim: 128, token_mlp_hidden_dim: 64, channel_mlp_hidden_dim: 256, num_mixer_blocks: 1, index_embedding: true, readout: mean, tm_mode: none}

