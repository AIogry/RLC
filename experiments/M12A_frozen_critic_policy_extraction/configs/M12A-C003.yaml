study_id: M12A
config_id: M12A-C003
slug: policy_extraction_single_state_k4_actor
algorithm: crl
environment: antmaze-large-navigate-v0
stage: policy_extraction
protocol_stage: stage2
placement: actor_single_state_k4
executable: true
description: SingleState K4 CRL actor against a same-seed frozen critic.
factors:
  training_mode: policy_extraction
  actor_topology: single_state
  actor_iterations: 4
  actor_residual: false
  critic_source: M12A-C001
dependencies:
  frozen_critic:
    source_config_id: M12A-C001
    seed_policy: same_seed
    source_run_attempt: 0
    checkpoint_role: last
    checkpoint_step: 1000000
    module: critic
agent_overrides:
  actor_loss: ddpgbc
  training_mode: policy_extraction
  runtime_variant: policy_extractor
  compute:
    actor:
      enabled: true
      primitive: mlp
      topology: single_state
      credit: direct
      topology_kwargs:
        iterations: 4
        residual: false
        input_injection: z_plus_x
        state_dim: 512
        state_init: normal_buffer
        state_init_std: 1.0
        update_depth: 2
        layer_norm: false
        update_activate_final: true
    critic_state: {enabled: false, primitive: mlp, topology: feedforward, credit: direct}
    critic_goal: {enabled: false, primitive: mlp, topology: feedforward, credit: direct}
    value_state: {enabled: false, primitive: mlp, topology: feedforward, credit: direct}
    value_goal: {enabled: false, primitive: mlp, topology: feedforward, credit: direct}
