study_id: M10A
config_id: M10A-C009
slug: hiql_alloc_h8_l8_b16
algorithm: hiql
placement: high_actor+low_actor
executable: true
description: HIQL SingleState balanced placement with K_H=8 and K_L=8 at matched B_body=16.
factors:
  high_iterations_K: 8
  low_iterations_K: 8
  body_compute_budget: 16
  high_budget_fraction: 0.5
  low_budget_fraction: 0.5
  topology: single_state
  primitive: mlp
  residual: false
  state_init: zero_buffer
  placement: high_actor+low_actor
agent_overrides:
  compute:
    high_actor: {enabled: true, primitive: mlp, topology: single_state, credit: direct, topology_kwargs: {iterations: 8, residual: false, input_injection: z_plus_x, state_dim: 512, state_init: zero_buffer}}
    low_actor: {enabled: true, primitive: mlp, topology: single_state, credit: direct, topology_kwargs: {iterations: 8, residual: false, input_injection: z_plus_x, state_dim: 512, state_init: zero_buffer}}
