study_id: M10A
config_id: M10A-C004
slug: hiql_alloc_h3_l2_b5
algorithm: hiql
placement: high_actor+low_actor
executable: true
description: HIQL SingleState placement with K_H=3 and K_L=2 at matched B_body=5.
factors:
  high_iterations_K: 3
  low_iterations_K: 2
  body_compute_budget: 5
  high_budget_fraction: 0.6
  low_budget_fraction: 0.4
  topology: single_state
  primitive: mlp
  residual: false
  state_init: zero_buffer
  placement: high_actor+low_actor
agent_overrides:
  compute:
    high_actor: {enabled: true, primitive: mlp, topology: single_state, credit: direct, topology_kwargs: {iterations: 3, residual: false, input_injection: z_plus_x, state_dim: 512, state_init: zero_buffer}}
    low_actor: {enabled: true, primitive: mlp, topology: single_state, credit: direct, topology_kwargs: {iterations: 2, residual: false, input_injection: z_plus_x, state_dim: 512, state_init: zero_buffer}}
