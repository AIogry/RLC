study_id: M9A
config_id: M9A-C002
slug: crl_vanilla
algorithm: crl
placement: baseline
executable: true
description: CRL DDPG+BC vanilla baseline; actor remains baseline and critic remains baseline.
factors:
  topology: feedforward
  block: mlp
  internal_iterations_K: null
  residual: null
  placement: baseline
agent_overrides:
  actor_loss: ddpgbc
  compute:
    actor: {enabled: false}
    critic_state: {enabled: false}
    critic_goal: {enabled: false}
    value_state: {enabled: false}
    value_goal: {enabled: false}
