study_id: M9A
config_id: M9A-C026
slug: hiql_high_low_k4_res
algorithm: hiql
placement: high_actor+low_actor
executable: true
description: HIQL high_actor and low_actor SingleState K=4 residual; value remains baseline.
factors: {topology: single_state, block: mlp, internal_iterations_K: 4, residual: true, placement: high_actor+low_actor}
agent_overrides:
  compute:
    high_actor: {enabled: true, primitive: mlp, topology: single_state, credit: direct, topology_kwargs: {iterations: 4, residual: true, input_injection: z_plus_x, state_dim: 512, state_init: normal_buffer, state_init_std: 1.0}}
    low_actor: {enabled: true, primitive: mlp, topology: single_state, credit: direct, topology_kwargs: {iterations: 4, residual: true, input_injection: z_plus_x, state_dim: 512, state_init: normal_buffer, state_init_std: 1.0}}
